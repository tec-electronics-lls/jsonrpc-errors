# Список ошибок сервисов JsonRPC

Должен быть валиден для всех сервисов и поддерживаться в актуальном состоянии.

Свойство `data` - информационное, для разработчика, может быть удалено (изменено) при использовании